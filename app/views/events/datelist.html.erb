<div> 
    <div class="col-md-5 offset-md-4"> 
        <h2 class="text-center pt-5"> Events </h2>
            <%= link_to '登録', new_admin_user_path, class: 'btn btn-primary' %> 
        <table class="table table-hover">
            <thead> 
                <tr>
                    <th>
                        <%= Event.human_attribute_name(:title) %> 
                    </th>
                    <th>
                        <%= Event.human_attribute_name(:start) %>
                    </th>
                    <th>
                        <%= User.human_attribute_name(:end) %>
                    </th>
                </tr>
            </thead>
            <tbody> 
                <% @events.each do |event| %>
                    <% if true %>
                        <tr> 
                            <td>
                                <%= link_to event.title, event %>
                            </td>
                            <td>
                                <%= link_to event.start, event %>
                            </td>
                            <td>
                                <%= link_to event.end, event %>
                            </td>
                        </tr>
                    <% end %>
                <% end %>
            </tbody>
        </table>
        <%= paginate @events %>
    </div>
</div>