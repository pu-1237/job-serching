<div> 
    <div class="col-md-5 offset-md-4"> 
        <h2 class="text-center pt-5"> 給与明細 </h2> 
        <table>
            <tr>
                <th>
                    スタッフNo
                </th>
                <td>
                    <%= current_user.id %>
                </td>
            </tr>
            <tr>
                <th>
                    氏名
                </th>
                <td>
                    <%= current_user.name %>
                </td>
            </tr>
        </table>
        <% today = Date.today %>
        <% 13.times do |i| %>
            <% d = today << i %>
            <%= link_to d.strftime('%Y年%m月'), "#{d.strftime('/events/payments/%Y/%m').to_s}" %><br>
        <% end %>
    </div>
</div>