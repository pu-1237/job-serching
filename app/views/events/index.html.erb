<div class="col-md-5 offset-md-3"> 

    <%# カレンダーの表示 %>
    <div id='calendar'></div>
    <%# app/javascript/packs/calendar/event.jsを読み込む %>
    <%= javascript_pack_tag 'calendar/event' %>

</div>
<div> 
    <div class="col-md-5 offset-md-4"> 
        <h2 class="text-center pt-5"> 新規案件 </h2>
            <% if current_user.admin? %>
                <%= link_to '追加', new_event_path, class: 'btn btn-primary' %> 
            <% end %>
        <table class="table table-hover">
            <thead> 
                <tr>
                    <th>
                        <%= Event.human_attribute_name(:title) %> 
                    </th>
                    <th>
                        <%= Event.human_attribute_name(:start) %>
                    </th>
                </tr>
            </thead>
            <tbody> 
                <% @events.each do |event| %>
                    <tr> 
                        <td>
                            <%= link_to event.title, event %>
                        </td>
                        <td>
                            <%= event.start.strftime("%m月%d日 %H時") %>
                        </td>
                    </tr>
                <% end %>
            </tbody>
        </table>
    </div>
</div>